#ifndef MULTI_PATTERN_SEARCH_H
#define MULTI_PATTERN_SEARCH_H

#include <map>
#include <cmath>
#include <vector>
#include <string>
#include <utility>
#include <iostream>
#include <algorithm>

using namespace std;

class MultiPatternSearch {
public:
  static int search(
      const string &text, const string &pattern);
};

#endif // MULTI_PATTERN_SEARCH_H
